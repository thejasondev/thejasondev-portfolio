{
  "name": "curso-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development astro dev",
    "start": "cross-env NODE_ENV=development astro dev",
    "build": "cross-env NODE_ENV=production astro build",
    "preview": "astro preview",
    "deploy": "cross-env NODE_ENV=production astro build && gh-pages -d dist"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/tailwind": "^5.1.4",
    "@fontsource-variable/source-code-pro": "^5.0.19",
    "astro": "^5.1.2",
    "tailwindcss": "^3.4.7",
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0",
    "prettier": "^3.4.2"
  }
}
