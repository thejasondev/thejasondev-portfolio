---
const projects = [
    {
        title: 'project 1',
        description: 'loren',
        link: '#',
        github: '#',
        image: '',
        tags: ['HTML','CSS','Js']
    }
]
---

{
    projects.map(({image, title,description,tags})=>(
    <article>
        <h3>{title}</h3>
        <p>{description}</p>
        <ul>
            {tags.map((tag)=>(
                <li>{tag}</li>
            ))}
        </ul>
        <img src={image} alt={`Captura de pantalla del proyecto ${image}`}/>
    </article>
    ))
}